@model IEnumerable<LocalIssueTracking>

@{
    ViewData["Title"] = "Index";
}

@if (TempData["ResultOk"] != null)
{
    <h1 class="alert-success">@TempData["ResultOk"]</h1>
}


<div class="container shadow p-5">

@*     <div class="col mb-3">
        <a asp-controller="Employee" asp-action="Create" class="btn btn-lg btn-primary"><i class="bi bi-file-plus-fill"></i>Add Employee</a>
    </div>
 *@    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th scope="col">IssueId</th>
                <th scope="col">IssueDesc</th>
                <th scope="col">States</th>
                <th scope="col">ScreenShot</th>
                <th></th>
            </tr>
        </thead>
        <tbody>

            @foreach (var item in Model)
            {
                <tr>
                    <td width="20%">
                        @item.IssueId
                    </td>
                    <td width="20%">
                        @item.IssueDesc
                    </td>
                    <td width="25%">
                        @item.States
                    </td>
                    <td width="20%">
                        <a href="@Url.Content("~/")@item.ScreenShotRelativePath" target="_blank">Issue Image</a>
                    </td>
@*                     <td width="15%">
                        @{
                            var logPath = Url.Content("~/") + @"Issues\5704362375234300001\Logs\MedDispense.UI.trace.log";//item.LogsRelativePath;
                            <a href="/IssuesTracking/download?filename =@logPath" class="btn btn-sm btn-success">log download. @logPath</a>
                        }
                    </td>
 *@                    <td width="15%">
                        @{
                            <a>  @Html.ActionLink("Log Download", "Download", new { fileName = @item.LogsRelativePath });</a>
                        }
                    </td>
                </tr>

            }
        </tbody>
    </table>
</div>
